---
label: Landscape Image
hide_body: true
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
  description: Title of the image is used in the alternate text.
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
- name: layout
  type: text
  config:
    required: true
  label: Layout
  hidden: true
  default: Landscape

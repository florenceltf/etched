---
label: Gallery item
hide_body: true
fields:
- name: alt
  type: text
  config:
    required: true
  label: Alternate Text
  description: Alternative text for the image. Usually a title or a name.
  default: Architecture image
- name: layout
  type: select
  default: Landscape
  config:
    required: true
    options:
    - Portrait
    - Landscape
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Layout
  description: How the image should be displayed
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
  showOnly:
    value: Portrait

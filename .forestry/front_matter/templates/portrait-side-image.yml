---
label: Portrait Side Image
hide_body: true
fields:
- name: alt
  type: text
  config:
    required: true
  description: Alternative text to display if the image fails to load.
  label: Alt
- name: image-left
  label: Image Left
  type: file
  config:
    maxSize: 64
    required: true
- name: caption-left
  type: text
  config:
    required: false
  label: Caption Left
- name: image-right
  label: Image Right
  type: file
  config:
    maxSize: 64
    required: true
- name: caption-right
  type: text
  config:
    required: false
  label: Caption Right

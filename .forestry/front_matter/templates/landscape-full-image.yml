---
label: Landscape Full Image
hide_body: true
fields:
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
- name: alt
  type: text
  config:
    required: true
  label: Alt
  description: Alternative text to display if the image fails to load.
- name: caption
  type: text
  config:
    required: false
  label: Caption
  description: Short description about the image.

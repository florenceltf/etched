---
label: Portrait Image
hide_body: true
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
  description: Title as used in the alternate text for the image
- name: layout
  type: text
  config:
    required: false
  label: Layout
  hidden: true
  default: portrait
- name: image
  type: file
  config:
    maxSize: 64
  label: Image

---
label: Portrait Image
hide_body: true
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
  description: Title as used in the alternate text for the image
- name: image
  type: file
  config:
    maxSize: 64
    required: true
  label: Image
